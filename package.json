{
  "name": "xefy",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "astro dev",
    "test": "node --test \"src/**/*.test.ts\"",
    "build": "npm run test && npm run check:content && astro build && npm run postbuild:index",
    "postbuild:index": "pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check",
    "check:content": "node scripts/integrity/run.ts",
    "check:self": "node scripts/integrity/run.ts --content test-fixtures/broken-content --expect-failure",
    "astro": "astro",
    "check:fixtures": "node scripts/integrity/run.ts --content test-fixtures/engine-content"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "@astrojs/svelte": "^9.0.1",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.0",
    "nanostores": "^1.4.2",
    "svelte": "^5.56.8",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "pagefind": "^1.5.2",
    "sharp": "^0.35.3",
    "tinyglobby": "^0.2.17",
    "yaml": "^2.9.0"
  }
}
