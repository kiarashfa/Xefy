# --- Build output ---
dist/
.astro/
node_modules/

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Environment ---
.env
.env.*
!.env.example

# --- Editor / OS cruft ---
.DS_Store
Thumbs.db
.idea/
.claude/

# --- Working notes ---
# Planning and handover documents. The build never reads them.
# CLAUDE.md points at MEMORY.md; MEMORY.md is the live project memory.
CLAUDE.md
MEMORY.md
# agent/ holds the authoring contract handed to content sub-agents, and the
# orchestrator's dispatch runbook.
agent/
# archive/ holds the superseded design and build documents.
archive/
ver7.html

# Editorial planning: the catalogue list and its supporting files. The build
# never reads them; they decide what gets written, not what gets shipped.
catalogue/

# Scratch analysis and checkup output. Build-critical scripts under scripts/
# are tracked; ad-hoc investigation is not.
scratch/
*.local.md

# Image curation scratch — downloaded candidates, reviewed then discarded.
image-review/
